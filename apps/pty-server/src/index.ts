import type { TerminalMessage as _TerminalMessage } from '@terminus-web/shared';

const PORT = process.env.PORT || 3001;

console.log(`PTY Server starting on port ${PORT}`);
console.log('Shared types imported successfully');
