# Terminus-Web Environment Configuration
# Copy this file to .env.local and modify as needed

# =============================================================================
# PORT CONFIGURATION
# =============================================================================

# Port for Next.js web frontend (default: 3000)
TERMINUS_WEB_PORT=3000

# Port for OpenCode serve backend (default: 3001)
OPENCODE_SERVE_PORT=3001

# =============================================================================
# OPENCODE CONFIGURATION
# =============================================================================

# Public URL for OpenCode serve (used by browser for WebSocket connection)
# In production: https://yourdomain.com
# In development: http://localhost:3001
NEXT_PUBLIC_OPENCODE_URL=http://localhost:3001

# Internal URL for server-side API proxy (Next.js to OpenCode)
# Usually http://localhost:PORT unless running in containers
OPENCODE_INTERNAL_URL=http://localhost:3001

# Optional: Command to run in PTY (defaults to user's shell)
# NEXT_PUBLIC_OPENCODE_COMMAND=/usr/local/bin/opencode

# =============================================================================
# GENERAL
# =============================================================================

# Environment mode
NODE_ENV=development
